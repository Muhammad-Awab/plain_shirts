const express = require('express');
const router = express.Router();
const Order = require('../models/orderModel');

// Define routes for order CRUD operations similar to productRoutes.js
// (Create, Read, Update, Delete)
