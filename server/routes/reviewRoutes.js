const express = require('express');
const router = express.Router();
const Review = require('../models/reviewModel');

// Define routes for review CRUD operations similar to productRoutes.js
// (Create, Read, Update, Delete)
