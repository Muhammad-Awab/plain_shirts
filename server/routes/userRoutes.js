const express = require('express');
const router = express.Router();
const User = require('../models/user');

// Define routes for user CRUD operations similar to productRoutes.js
// (Create, Read, Update, Delete)
